# PAUSE POINT — 2026-07-28 13:25 EDT (candidate-expansion tranche)

Session ran ~11:18 → 13:25 EDT. Mid-session the whole `ip-eval/` dir was deleted by an
external Claude session (accident) and restored by it; restore verified complete — including
this tranche's 10 packs and the fix-agent's annoy/turso/opendal edits. Venvs were NOT
restored; they re-provision on demand.

## State of the approved plan (plan file: session plans dir, "Rewire ip-eval races…")

- **Phase 0 DONE** — tabilify race fixed (`tabilify.py` gained `tabilify_table()`;
  `race.py:_workload_tabilify` docs path bug fixed to use gold `formats.md.file`).
  Green: IP 0.5614 > md-table-parser/genie/openscience/sheetjs 0.4773 > naive 0.3518.
- **Phase 1 DONE** — 10 pack briefs at `evo-myworld/racetrack/requests/ip-eval-packs/`.
- **Phase 2 DONE** — all 10 packs built by subagents into `ip_eval/candidates_extra/`;
  registry ~25 → ~70 candidates; selftest green; `cli list` clean.
- **Phase 3 IN PROGRESS** — first acceptance pass ran all 12 stages (results-*-runN.json,
  latest N per stage). 4 defects found:
  - annoy (missing numpy in PIP) — FIXED (survived restore).
  - turso-store / opendal-store (`nonlocal` SyntaxError + libsql lock retry) — FIXED (survived).
  - hayhooks-api (server never came up on 8973; stderr was DEVNULL'd) — **being fixed now**
    by coder subagent agent-12 (resume id: agent-12).
  - nemo-gliner (TimeoutExpired escaping from concepts(); needs HF_HUB_OFFLINE=1 +
    exception-proof available()) — **being fixed now** by same subagent.
- **Phase 4 PENDING** — final race-all, then update `racetrack/results/ip-suite.md`, INDEX.md
  T6 row, this HANDOFF (tranche 4), `information-processer/RACETRACK-RESULTS-HANDOFF.md`,
  and a dated FIELD-NOTES.md entry.

## Running right now (do not assume finished — check before resuming)

1. Background bash: sequential `race split → retrieval → persistence` (split done, same
   scores as before; retrieval re-runs because haystack venv now exists for haystack-bm25;
   persistence validates turso/opendal fixes).
2. Subagent agent-12: hayhooks-api + nemo-gliner fixes in `candidates_extra/`.

## Resume steps

1. Read the background race log + agent-12's report; then re-run `race api` and
   `race concepts` (they depend on agent-12's fixes).
2. `python3 -m ip_eval.cli selftest` must stay green.
3. Do Phase 4 (docs above).
4. Deliberately NOT re-run: `race extract` (new extract candidates are all honest-unavailable;
  MinerU re-provision is multi-GB, no new information). Frontier unchanged: OCR image lane,
  docker lane (qdrant/ES/couchdb/meili/airflow), R10 SLO/fault rows.

## Known-good leaderboard anchors (post-expansion, from run logs)

- drafting: paperspine-draft 0.9325 > IP 0.9077 (IP lost its crown here).
- graph: graphify-graph/naive/networkx 0.9833 > IP 0.9714.
- retrieval: 5-way tie 0.3564 (IP, librer, ragbits-rrf, rank-bm25, tantivy-py).
- persistence: IP = sqlite 0.85 > pouchdb 0.775.
- Most new corpus candidates record honest-unavailable with true reasons (LLM-bound,
  java/cargo/platform-gated, or mapping mismatches like doxx/mdx-rs being reader-only).
