# virtualenv + node deps (rebuild via HANDOFF.md; ~900MB)
.venv/
node_modules/
package-lock.json
# python caches
__pycache__/
*.pyc
# run outputs + regenerable fixtures (cert is regenerated by hardened.ensure_cert)
out/
fixtures/hardened/
# keep: crawl_eval/*.py, fixtures/site1/**, results-*.json, pipeline-*.json,
#       HANDOFF.md, .gitignore
